class L {
  static void i(Object? v) {
    print("[I]: " + (v??"null").toString());
  }
  static void e(Object? v) {
    print("[E]: " + (v??"null").toString());
  }
  static void w(Object? v) {
    print("[W]: " + (v??"null").toString());
  }
}
